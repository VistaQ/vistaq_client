# Override staging API URL so MSW can intercept all fetch calls during tests.
# apiClient.ts uses: import.meta.env.VITE_API_URL || '/api'
# Empty string here forces the fallback to '/api' (relative), which MSW handles.
VITE_API_URL=
VITE_TENANT_SLUG=test-tenant
